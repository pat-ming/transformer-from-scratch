# Transformer from Scratch

A transformer implementation using only NumPy — no deep learning frameworks.

## Setup

```bash
python -m venv venv
source venv/bin/activate
pip install numpy
```

## Usage

```bash
python main.py
```
